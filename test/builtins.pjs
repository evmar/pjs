(if a b c)

(if a
  (if c
    (if d e)
    f))

(var a)

(var a (+ 3 4))

(function (x) (+ 1 x))
(function foo (bar baz)
  1 2)

(function foo (bar baz)
  (return (+ x 1)))

(for (var i 0) (< i 4) (++ i)
  (return i))
(throw "foo")
(break)
(continue)

(var x (new y))
(new y z)

(at x (+ 1 2))

(if x
  (do y z))
